type: object
allOf:
  - type: object
    required:
      - name
      - percents
    properties:
      error:
        description: Тип ошибки
        type: string
      percents:
        description: Относительное колличество ошибок в процентах
        type: integer
        format: int32
        minimum: 0