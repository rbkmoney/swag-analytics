type: object
allOf:
  - type: object
    required:
      - amount
      - currency
    properties:
      currency:
        x-merge-obj:
          - $ref: '#/definitions/Currency'
      offsetAmounts:
        type: array
        items:
          $ref: '#/definitions/OffsetAmount'