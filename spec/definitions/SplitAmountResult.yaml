type: object
properties:
  splitUnit:
    x-merge-obj:
      - $ref: '#/parameters/splitUnit'
  currency:
    x-merge-obj:
      - $ref: '#/definitions/Currency'
  offsetAmounts:
    type: array
    items:
      $ref: '#/definitions/OffsetAmount'