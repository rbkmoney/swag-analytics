type: object
properties:
  splitUnit:
    x-merge-obj:
      - $ref: '#/parameters/splitUnit'
  currency:
    x-merge-obj:
      - $ref: '#/definitions/Currency'
  statusOffsetCounts:
    type: array
    items:
      $ref: '#/definitions/StatusOffsetCount'