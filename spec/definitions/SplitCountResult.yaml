type: object
allOf:
  - type: object
    required:
      - offset
      - count
      - currency
    properties:
      currency:
        x-merge-obj:
          - $ref: '#/definitions/Currency'
      offsetCounts:
        type: array
        items:
          $ref: '#/definitions/StatusOffsetCount'