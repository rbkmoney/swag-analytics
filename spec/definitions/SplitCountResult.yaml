type: object
allOf:
  - type: object
    required:
      - currency
      - statusOffsetCounts
    properties:
      currency:
        x-merge-obj:
          - $ref: '#/definitions/Currency'
      statusOffsetCounts:
        type: array
        items:
          $ref: '#/definitions/StatusOffsetCount'